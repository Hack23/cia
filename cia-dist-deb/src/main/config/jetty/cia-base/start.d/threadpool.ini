# ---------------------------------------
# Module: threadpool
# Enables and configures the Server ThreadPool.
#
# Tuned for the CIA application:
#   * Vaadin Push / Atmosphere long-poll sessions
#   * Bursty Riksdagen / ESV ingestion jobs
#
# Note on memory:
# Thread stacks are allocated in native (off-heap) memory and their size
# is JVM/platform-dependent (typically ~1MB on Linux unless -Xss is set).
# They are NOT drawn from the Java heap.
#
# The canonical location for JVM heap (-Xmx/-Xms) and stack (-Xss) flags
# is /etc/default/cia (JAVA_OPTIONS / JETTY_ARGS), sourced by the
# systemd unit on start. Keep the sizing here aligned with whatever
# values /etc/default/cia actually sets.
# ---------------------------------------
--module=threadpool

## Minimum number of pooled threads.
jetty.threadPool.minThreads=20

## Maximum number of pooled threads.
jetty.threadPool.maxThreads=400

## Number of reserved threads (-1 for heuristic).
jetty.threadPool.reservedThreads=8

## Thread idle timeout (in milliseconds).
jetty.threadPool.idleTimeout=60000

## Whether to output a detailed dump.
#jetty.threadPool.detailedDump=false
